/*
este exemplo serve para demonstrar
as funcoes info() e compare()
*/

// nota: na pasta res/ estão as imagens
// dos automatos usados em testes
A = read("../res/empty_string.dot");
B = read("../res/empty_language.dot");

// a funcao info() verifica o
// determinismo de um automato e
// se este representa a cadeia
// vazia ou a linguagem vazia
info(A);
info(B);

// a função compare() testa
// a relação entre as linguagens
// de dois automatos
compare(A,B);